# 런타임 · 설정
config.json
bk_cb.json
.bot.pid
# .mcp.json 은 개발자 로컬 MCP 설정 (server.ts 절대 경로 포함, 머신마다 다름).
# 배포/설치 시엔 bin/setup.sh 가 user-scope 에 등록하므로 커밋 불필요.
.mcp.json

# Bun / Node
node_modules/
bun.lockb

# 로그 · 덤프
logs/
dump/
capture/
*.log

# 텔레그램 이미지 캐시 (legacy)
tg_images/

# Python 잔재 (legacy)
venv/
__pycache__/
*.pyc
.pytest_cache/

# 편집기
.vscode/
.idea/
.DS_Store
